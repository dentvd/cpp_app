#include<iostream>
#include<time.h>
#include<chrono>
#include<random>
#include<stdio.h>
using namespace std;
int main()
{
	random_device rd;
	uni
	int i = 0;
	srand(time(0));
	int a,b;
	a = rand() % 10;
	srand(time(0));
	b = rand() % 10;
	cout << a;
	cout << b;
	return 0;
}